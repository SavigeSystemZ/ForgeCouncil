# Skill: Validation gate

## Steps

1. Load `gate_result` schema.  
2. Run declared commands from task packet / `REPO_PROFILE.md`.  
3. Capture logs as artifacts; attach checksums.  
4. On failure: structured failure object to Debugger role.

## Stub script

- `bootstrap/fc-gate-check.sh`
