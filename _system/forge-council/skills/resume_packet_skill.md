# Skill: Resume packet export

## Steps

1. Gather L1 snapshot: active milestone, open questions, last run id.  
2. Gather L2 pins: decision IDs, canonical doc versions.  
3. Run `bootstrap/fc-export-resume-packet.sh <repo>`.  
4. Update `WHERE_LEFT_OFF.md` consistently.
