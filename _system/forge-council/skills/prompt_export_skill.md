# Skill: Prompt pack export

**Contract:** `_system/PROMPT_EMISSION_CONTRACT.md`

## Steps

1. Choose pack from `_system/prompt-packs/forge-council/`.  
2. Prepend host-safe preamble (load `AGENTS.md`, precedence contract, profiles).  
3. Inject task-specific paths only; avoid full rule duplication.  
4. Record export version in cover section.
