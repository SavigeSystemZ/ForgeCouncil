# Context: Integrations

- **Models:** adapter layer; prefer Responses-API-shaped flows for OpenAI; parallel adapters for others.  
- **MCP:** workspace manifest; policy-bound.  
- **Git/CI:** branch-scoped git; CI adapters for validation gate.

See `ARCHITECTURE.md` integration plane.
