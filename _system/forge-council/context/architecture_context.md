# Context: Architecture

Modular monolith control plane + **isolated runners**. No runtime imports from `_system/`. JSON Schemas under `schemas/forge_council/v1/` are interchange SSoT.

State machine and diagrams: `ARCHITECTURE.md`.
